# 선택: 기본 저장소는 src/siteConstants.js 의 DEFAULT_SOURCE_REPO_URL 과 동일.
# 포크·미러만 쓸 때 아래로 덮어쓴다 (끝 슬래시·.git 없음).
# VITE_SOURCE_REPO_URL=https://github.com/other-org/other-repo
# 기본 브랜치가 main이 아니면:
# VITE_SOURCE_REPO_BRANCH=main
